import { Header } from '@components/Header/Header';
import './Home.scss';
import React, { type FC } from 'react';

export const Home: FC = () => {
  return (
    <div className="">
      <Header/>
    </div>
  );
};

export default Home;
