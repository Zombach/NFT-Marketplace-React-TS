import './Home.scss';
import Button from '@components/Button/Button';
import React, { type FC } from 'react';

export const Home: FC = () => {
  return (
    <div className="">
      <Button>T</Button>
    </div>
  );
};

export default Home;
