import './Footer.scss';
import React, { type FC } from 'react';

export const Footer: FC = () => {
  return (
    <div className="footer">
      <svg></svg>
    </div>
  );
};

export default Footer;
